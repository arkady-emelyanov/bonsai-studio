{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Settings window talks to the Rust core over IPC only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
